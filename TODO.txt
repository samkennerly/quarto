ASAP:
  - styles: standardize. include gallery tweaks.
  - style/elemental: use github.com/neocities/element/element.css ?
  - option: arrow text for prev + next in #icons
  - option: stylesheets: ['style.css']
    quarto/apply style/adrift target/style.css
  - .clean() should not require index.html ?
  - move site to neocities. update index.json.
  - quarto/export: sync target to neocities account?

QUARTO:
  - can urlpath() accept external links?
  - background: relative path in style.css ?
  - option stylesheets: [style.css, http://whatever/style.css, ...]
  - option pages: [About, Photo/Editorial, Photo/Headshots, ...]
  - use page titles for nav buttons?
  - does tidy kill <style> section inside <main> ?
  - markdown: use mistune.py ?
  - `gallerize` builds image galleries?
  - multithreading: one page per core?
  - tidylib C bindings (not subprocess)
  - Bottle or Flask demo app?

README:
  - no install required
  - no __init__.py, __main__.py, setup.py, sys.path, PYTHONPATH, pip, venv
  - folders: { quarto: code, HTML: ready, CSS: style, media: target }
  - delete Dockerfile, kitchen if you don't want Docker
  - gotcha: spaces & weird chars in filenames
  - gotcha: can't delete empty folders because .DS_Store
  - gotcha: inline <style> requires <body> tag (if using Tidy)
  - standard workflow:
    0: write pages in preprint
    1: copy pages to ready
    2: quarto/delete html target
    3: quarto/clean ready ready
    4: quarto/build ready target
    5: quarto/apply style/doctoral target

STYLES:

  - download PNGs from subtlepatterns.com

TESTING:
  - test caps, spaces, underscores in page paths
  - what happens if old tidy is called?
  - nav: arrow keys, disability access OK?
  - speed tests: compare to Hugo, Jekyll, Middleman?

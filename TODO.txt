ASAP:
  - quarto/delete. clear empty folders with .unlink()?
  - quarto/clean. use tidybody(). never modify inputs.
  - finish class Quarto docstring
  - icons for all 4 styles
  - reserve PyPA name?

HOSTING:
  - find static host. update index.json.
  - test caps, spaces, underscores in page paths
  - fork into multiple sites?

QUARTO:
  - test with older Python versions
  - markdown: use mistune.py ?
  - `gallerize` builds image galleries?
  - multithreading: one page per core?
  - Bottle or Flask demo app?
  - call tidylib C functions

README:
  - no install required
  - no __init__.py, __main__.py, setup.py, sys.path, PYTHONPATH, pip, venv
  - code in quarto/
  - HTML in ready/
  - CSS in style/
  - other files in target/ folder. build does not copy files.
  - delete Dockerfile, kitchen if you don't want Docker
  - gotcha: spaces & weird chars in filenames

STYLES:
  - external stylesheets in index.json?
  - easiest way to include background image?
  - download PNGs from subtlepatterns.com

TESTING:
  - speed tests! compare to Hugo, Jekyll, Middleman?
  - what happens if old tidy is called?
  - nav: arrow keys, disability access OK?

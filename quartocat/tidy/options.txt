// http://www.html-tidy.org/documentation/

accessibility-check: 0          // Priority level. (Default is 0.)
add-meta-charset: y             // Ensure <meta charset=...> tag exists?
anchor-as-name: y               // Add name to potential anchor elements?
ascii-chars: n                  // If clean, then convert entities to ASCII?
bare: n                         // Remove Microsoft Word cofvefe?
break-before-br: y              // Line break before each <br>?
clean: y                        // Replace surplus tags with style rules?
coerce-endtags: n               // Add / to tags which look like end tags?
css-prefix: c                   // Prefix for Tidy styles?
custom-tags: n                  // Render weird tags?
decorate-inferred-ul: n         // Add CSS markup to inferred <ul> elements?
doctype: html5                  // Ensure <!DOCTYPE html> tag exists?
drop-empty-elements: y          // Remove empty elements?
drop-empty-paras: y             // Remove empty paragraphs?
drop-proprietary-attributes: y  // Remove weird attributes?
enclose-block-text: n           // Insert <p> if HTML strict requires it?
enclose-text: n                 // Move free text into <p> element(s)?
escape-scripts: y               // Replace items like </g with <\/g ?
fix-backslash: y                // Replace \ -> / in URLs?
fix-bad-comments: auto          // Replace unexpected hyphens with = chars?
fix-style-tags: y               // Move all <style> tags inside <head>?
fix-uri: y                      // Escape illegal characters in URIs?
force-output: n                 // Output page even if Tidy raises errors?
gdoc: n                         // Use Google Docs HTML parser?
hide-comments: n                // Remove comments from output?
indent: n                       // Indent block-level tags?
indent-attributes: n            // New line for each attribute?
indent-spaces: 2                // Indent size?
indent-with-tabs: n             // Indent with tabs?
input-encoding: utf8            // Assume encoding of input page?
input-xml: n                    // Use Tidy's XML parser?
join-classes: n                 // Assign multi-class elements to one new class?
join-styles: y                  // Assign multi-style elements to one new style?
keep-tabs: n                    // Preserve tab characters?
keep-time: n                    // Preserve file modification time?
literal-attributes: n           // Avoid normalizing whitespace in attrs?
logical-emphasis: y             // Replace <i> -> <em> and <b> -> <strong>?
lower-literals: y               // Force predefined attr values to lowercase?
markup: y                       // Output tidy version of page?
merge-divs: auto                // If clean, then remove nested divs?
merge-emphasis: y               // Merge nested <b> and <i> elements?
merge-spans: auto               // If clean, then remove nested spans?
ncr: y                          // Allow numeric character references?
newline: LF                     // Line ending type?
numeric-entities: n             // Output non-HTML entities in numeric form?
omit-optional-tags: y           // Remove optional start and/or end tags?
output-bom: n                   // Start output with unicode Byte Order Mark?
output-encoding: utf8           // Force output to this encoding?
output-html: y                  // Force output to HTML?
output-xhtml: n                 // Force output to XHTML?
output-xml: n                   // Force output to XML?
preserve-entities: n            // Preserve well-formed entities?
punctuation-wrap: n             // Wrap line after certain Unicode characters?
quiet: y                        // Output only errors, warnings, and page?
quote-ampersand: y              // Replace & with &amp; ?
quote-marks: n                  // Replace " with &quot; ?
quote-nbsp: y                   // Replace with non-breaking space with &nbsp ?
repeated-attributes: keep-last  // Keep first or last of conflicting attrs?
replace-color: n                // Replace numeric colors with names?
show-body-only: n               // Output <body> contents only?
show-errors: 6                  // Error log level? (0 ignres all errors.)
show-info: y                    // Output info-level logs?
show-meta-change: n             // Display message when meta content changed?
show-warnings: y                // Output warning-level logs?
skip-nested: y                  // Skip nested tags in script and style data?
sort-attributes: none           // Sort attrs within an element?
strict-tags-attributes: y       // Raise error for weird attrs?
tab-size: 2                     // How many spaces per tab?
tidy-mark: y                    // Add <meta> tag indicating use of Tidy?
uppercase-attributes: n         // Force attribute names to uppercase?
uppercase-tags: n               // Force tag names to uppercase?
vertical-space: y               // Add empty lines for readability?
warn-proprietary-attributes: y  // Warn if weird attrs found?
wrap: 86                        // Max characters per line?
wrap-attributes: n              // Wrap attribute values?
wrap-script-literals: no        // Wrap literals in script attrs?
write-back: n                   // Overwrite input file?
